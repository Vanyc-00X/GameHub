import 'package:flutter/material.dart';

class BottomShop extends StatelessWidget {
  const BottomShop({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Магазин", style: TextStyle(fontSize: 30, color: Colors.white)));
}